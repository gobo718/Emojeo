EMOJEO PASS 40 — STEP 3 POPULATION BATCH

Adds:
- 857-relationship machine inventory
- 79-subject diverse Batch 001
- 81-domain bounded run spec
- resumable Step 3 population runner
- Step 3 run/stop/download UI

Install from repository root:
  unzip -o Emojeo_Pass40_STEP3_POPULATION_FLAT.zip
  python install-step3.py

The patcher changes index.html only at two exact anchors and aborts without writing if either anchor is absent.
No Worker bindings, provider routing, semantic-pilot files, or existing geometry are modified.
